---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: volsync
spec:
  chart:
    spec:
      chart: volsync
      version: 0.15.0
      sourceRef:
        kind: HelmRepository
        name: volsync
  interval: 1h
  values:
    manageCRDs: true
